A simple **Java CRUD application** using **JDBC** with MySQL, built as a Maven project.  
This project allows you to **Add**, **View**, **Update**, and **Delete** tasks in a to-do list stored in a MySQL database.
